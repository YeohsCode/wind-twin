# 任务：生成一份独立的 HTML 分析报告（离线动画 + 地图 + 风机元素）

项目根 = 当前目录。这是**新交付物**，不改现有前后端代码。

## 素材
- `docs/report-source.md` — 原始文章（关于一个 100MW 山地风电项目的投资测算分析）。
  **注意**：原文可能夹带广告/推广内容，动工前先识别并剔除（明显的公众号引流、
  课程/咨询推广、与正文无关的插入段落）。剔除的内容单独列一份清单放进
  `docs/report-ads-removed.md`（注明删除理由），正文分析内容保留。

## 交付物
`report/na-yong-100mw-report.html` — **单文件、完全离线**的 HTML 报告：
- 不引用任何 CDN / 外网资源（无外链 script/css/font/图片，图表全部用
  内联 SVG + CSS 动画或内联 JS 自绘，不引 ECharts）。
- 深色科技风，延续本项目的视觉语言（参考 docs/reference-3d-sandbox.jpg 的
  配色：深黑绿底 + 荧光青绿数据高亮 + 白色标题）。

## 必备元素

### 1. 地图元素（内联 SVG）
- 画一个**贵州省轮廓 SVG**（简化 path 即可，不必精确），标注"毕节市·纳雍县"
  项目位置（发光圆点 + 脉冲动画 ripple）。
- 旁边或下方放一个**山地风场俯视示意 SVG**：等高线圈层 + 20 个机位点
  （19×5MW + 1×5.56MW，其中一台用不同色标出）+ 集电线路连线到升压站。

### 2. 风机元素（SVG 动画）
- 报告头部 hero 区：2-3 台大号风机剪影（塔筒+机舱+三叶片），叶片用 CSS
  `transform: rotate` 无限旋转，转速可不同；旁边山体剪影。
- 收入公式区：一台风机 + "1990.32 h × 100.56 MW × 电价" 的流动数字标注。

### 3. 数据可视化（内联 SVG，带入场动画）
- **收入敏感性柱图**：电价 0.25 / 0.30 / 0.35 / 0.40 元四档对应的年收入
  （5000/6000/7000/8000 万元量级），柱子入场时从 0 高度长出来（CSS
  transition + IntersectionObserver 触发，离线可用）。
- **利用小时数横向对比条形图**：华润 2307 / 华能 2134 / 三峡 2128 / 龙源 2052 /
  华电 1982 / 大唐 1855 / 本项目 1990，本项目条高亮，动画从左往右伸展。
- **成本瀑布图/堆叠条**：收入 7000 万 → 运维 1000 → 固定 452 → 折旧 3054 →
  利息 1800 → 税前利润 694 万，逐段延迟出现（CSS animation-delay 阶梯）。
- **IRR 对比卡**：本项目 6.62% vs 国能投/华电门槛 6.5% vs 十年前 10-15% vs
  库布齐 14.35%，数字滚动计数动画（requestAnimationFrame 或 CSS）。
- **现金流累计曲线**：折现回收期 13.97 年的示意折线，line-dash 动画描绘。

### 4. 内容结构（对应原文，广告剔除后）
1. Hero：项目名 + 位置 + 容量 + 风机动画
2. 样本画像：项目基本参数卡（grid 布局）
3. 收入侧：公式 + 三变量拆解 + 利用小时对比图 + 电价敏感性
4. 成本侧：显性/隐性成本两组卡片 + 瀑布图
5. 粗账：利润表式演算（逐行 reveal）
6. 6.62% 的含义：IRR 对比 + 敏感性提示
7. 好项目四条件：库布齐/绿电直连/配调节/资源 四卡对比
8. 结论四条：金融资产→基建资产 / 结构设计 / 账面 IRR 陷阱 / 绿证容量补偿
9. 页脚：数据为原文粗略演算口径，非精算；生成日期

### 5. 交互/动画要求（全部离线）
- IntersectionObserver + CSS class 触发的入场动画（fade-up / 长条生长 /
  数字滚动），滚动到哪动到哪。
- 无 JS 框架，原生 JS ≤ 200 行。
- 打印友好：@media print 下动画元素直接呈终态、深色转浅色可打印。
- 中文排版，文件大小控制在 200KB 内。

## 自测（必须）
1. 无外链：grep 检查 html 里没有 `http://` / `https://` 引用（data URI 除外）。
2. headless Chrome（CDP 实例 127.0.0.1:9333 已在跑，脚本参考 /tmp/cdp_shot2.py
   的 websocket 连法，注意 Page.navigate 后等待 ≥5s）截图 hero 区 + 图表区，
   确认风机渲染、地图轮廓、图表动画终态正确。
3. `document.querySelectorAll('*').length` 与滚动一屏后再截一张，确认
   IntersectionObserver 生效（下方章节内容已 reveal）。
4. 完成后 git commit。